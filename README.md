For the ZJ Beny (non-OCCP)

Building on the fine work of Jarauvi

What's working now:

✅ All DLB sensors showing correct values
✅ grid_power correctly handles negative values (exporting)
✅ solar_power, house_power, ev_power all accurate
✅ Retry logic with timeout for reliability
✅ New set_max_current service (6-32A)
✅ Single grid_power sensor (not split into import/export)
